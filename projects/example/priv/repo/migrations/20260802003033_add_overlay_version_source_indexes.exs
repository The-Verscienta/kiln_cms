defmodule KilnCMS.Repo.Migrations.AddOverlayVersionSourceIndexes do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create index(:faqs_versions, [:org_id, :version_source_id, :version_inserted_at, :id])

    create index(:testimonials_versions, [:org_id, :version_source_id, :version_inserted_at, :id])

    create index(:products_versions, [:org_id, :version_source_id, :version_inserted_at, :id])

    create index(:team_members_versions, [:org_id, :version_source_id, :version_inserted_at, :id])
  end

  def down do
    drop_if_exists index(:team_members_versions, [
                     :org_id,
                     :version_source_id,
                     :version_inserted_at,
                     :id
                   ])

    drop_if_exists index(:products_versions, [
                     :org_id,
                     :version_source_id,
                     :version_inserted_at,
                     :id
                   ])

    drop_if_exists index(:testimonials_versions, [
                     :org_id,
                     :version_source_id,
                     :version_inserted_at,
                     :id
                   ])

    drop_if_exists index(:faqs_versions, [:org_id, :version_source_id, :version_inserted_at, :id])
  end
end
