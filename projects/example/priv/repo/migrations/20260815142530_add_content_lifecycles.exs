defmodule KilnCMS.Repo.Migrations.AddContentLifecycles do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:team_members) do
      add :expiry_action, :text, null: false, default: "unpublish"
      add :review_after_days, :bigint
      add :last_reviewed_at, :utc_datetime_usec
    end

    create index(:team_members, [:org_id, :last_reviewed_at],
             name: "team_members_last_reviewed_at_index",
             where: "review_after_days IS NOT NULL AND state = 'published'"
           )

    create index(:team_members, [:org_id, :unpublish_at],
             name: "team_members_unpublish_at_index",
             where: "unpublish_at IS NOT NULL AND state = 'published'"
           )

    alter table(:testimonials) do
      add :expiry_action, :text, null: false, default: "unpublish"
      add :review_after_days, :bigint
      add :last_reviewed_at, :utc_datetime_usec
    end

    create index(:testimonials, [:org_id, :last_reviewed_at],
             name: "testimonials_last_reviewed_at_index",
             where: "review_after_days IS NOT NULL AND state = 'published'"
           )

    create index(:testimonials, [:org_id, :unpublish_at],
             name: "testimonials_unpublish_at_index",
             where: "unpublish_at IS NOT NULL AND state = 'published'"
           )

    alter table(:conditions) do
      add :expiry_action, :text, null: false, default: "unpublish"
      add :review_after_days, :bigint
      add :last_reviewed_at, :utc_datetime_usec
    end

    create index(:conditions, [:org_id, :last_reviewed_at],
             name: "conditions_last_reviewed_at_index",
             where: "review_after_days IS NOT NULL AND state = 'published'"
           )

    create index(:conditions, [:org_id, :unpublish_at],
             name: "conditions_unpublish_at_index",
             where: "unpublish_at IS NOT NULL AND state = 'published'"
           )

    alter table(:faqs) do
      add :expiry_action, :text, null: false, default: "unpublish"
      add :review_after_days, :bigint
      add :last_reviewed_at, :utc_datetime_usec
    end

    create index(:faqs, [:org_id, :last_reviewed_at],
             name: "faqs_last_reviewed_at_index",
             where: "review_after_days IS NOT NULL AND state = 'published'"
           )

    create index(:faqs, [:org_id, :unpublish_at],
             name: "faqs_unpublish_at_index",
             where: "unpublish_at IS NOT NULL AND state = 'published'"
           )
  end

  def down do
    drop_if_exists index(:faqs, [:org_id, :unpublish_at], name: "faqs_unpublish_at_index")

    drop_if_exists index(:faqs, [:org_id, :last_reviewed_at], name: "faqs_last_reviewed_at_index")

    alter table(:faqs) do
      remove :last_reviewed_at
      remove :review_after_days
      remove :expiry_action
    end

    drop_if_exists index(:conditions, [:org_id, :unpublish_at],
                     name: "conditions_unpublish_at_index"
                   )

    drop_if_exists index(:conditions, [:org_id, :last_reviewed_at],
                     name: "conditions_last_reviewed_at_index"
                   )

    alter table(:conditions) do
      remove :last_reviewed_at
      remove :review_after_days
      remove :expiry_action
    end

    drop_if_exists index(:testimonials, [:org_id, :unpublish_at],
                     name: "testimonials_unpublish_at_index"
                   )

    drop_if_exists index(:testimonials, [:org_id, :last_reviewed_at],
                     name: "testimonials_last_reviewed_at_index"
                   )

    alter table(:testimonials) do
      remove :last_reviewed_at
      remove :review_after_days
      remove :expiry_action
    end

    drop_if_exists index(:team_members, [:org_id, :unpublish_at],
                     name: "team_members_unpublish_at_index"
                   )

    drop_if_exists index(:team_members, [:org_id, :last_reviewed_at],
                     name: "team_members_last_reviewed_at_index"
                   )

    alter table(:team_members) do
      remove :last_reviewed_at
      remove :review_after_days
      remove :expiry_action
    end
  end
end
