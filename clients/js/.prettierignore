dist/
node_modules/
package-lock.json
# The parity golden must stay byte-identical to what both emitters produce —
# reformatting it would break the golden test on both sides.
test/fixtures/golden.d.ts

# Generated by scripts/generate_image_transform_vectors.exs and asserted
# verbatim by three suites — reformatting it changes nothing but the diff.
test/fixtures/image_transform_vectors.json
