{
  "_comment": [
    "Line-coverage settings for `mix coveralls.*` (excoveralls), #1314.",
    "",
    "`minimum_coverage` is a FLOOR, not a target: it sits just under the number",
    "the full suite measured when it was last moved, so coverage cannot regress",
    "silently. When a PR raises the measured number by a whole point or more,",
    "raise this to just under the new number in the same PR. Never lower it to",
    "make a red build green — that is the regression it exists to surface.",
    "",
    "`skip_files` are regexes on the source path. test/support is compiled",
    "into the app under MIX_ENV=test (see elixirc_paths/1 in mix.exs) but is",
    "test scaffolding, not code under test. Modules a dependency's macro",
    "generates into this app (Spark DSL extensions, AshGraphql subscription",
    "config, Postgrex type module, …) report under the dependency's own",
    "source file — an absolute path into deps/ — and are that dependency's",
    "code, not ours.",
    "",
    "Measured 2026-09-17 on `main` (a578f60e), after the coverage batches for",
    "`Media.Ingest` (#1502), `Storage.S3` (#1507) and the portability CLI and",
    "its three mix tasks (#1510): CI's own Coverage (full suite) job read",
    "85.0%, and the same suite read 84.6% locally over 8,537 tests — the gap",
    "being the capability-gated tags a runner skips and a developer machine",
    "does not (:qpdf, and the :ffmpeg / :no_ffmpeg pair, which invert with the",
    "host). The floor moves 82.7 -> 84.5 here, keeping the half-point margin",
    "the previous move used, because two batches had landed without it moving",
    "and the slack under CI's number had grown to 2.3 — room enough to lose",
    "several modules' coverage without the gate noticing. The floor is set from",
    "the CI number, that being the environment that enforces it. Re-state this",
    "measurement whenever you move the floor, so the next reader can tell what",
    "the floor was actually checked against.",
    "",
    "terminal_options.print_files: the per-file table is off; the per-directory",
    "rollup (`mix kiln.coverage.summary`) is the readable view, and the HTML",
    "report has every file. `mix coveralls.detail` still prints sources."
  ],
  "skip_files": [
    "^test/support/",
    "(^|/)deps/"
  ],
  "coverage_options": {
    "treat_no_relevant_lines_as_covered": false,
    "output_dir": "cover/",
    "minimum_coverage": 84.5,
    "floor_coverage": true
  },
  "terminal_options": {
    "print_files": false
  }
}
