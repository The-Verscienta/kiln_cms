defmodule KilnCMS.Repo.Migrations.AddMissedPaths do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:missed_paths, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :org_id,
          references(:organizations,
            column: :id,
            name: "missed_paths_org_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :path, :text, null: false
      add :locale, :text, null: false, default: "en"
      add :count, :bigint, null: false, default: 1
      add :last_seen_at, :utc_datetime_usec, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:missed_paths, [:org_id, :path, :locale],
             name: "missed_paths_unique_missed_path_index"
           )
  end

  def down do
    drop constraint(:missed_paths, "missed_paths_org_id_fkey")

    drop_if_exists unique_index(:missed_paths, [:org_id, :path, :locale],
                     name: "missed_paths_unique_missed_path_index"
                   )

    drop table(:missed_paths)
  end
end
