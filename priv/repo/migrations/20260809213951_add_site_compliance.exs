defmodule KilnCMS.Repo.Migrations.AddSiteCompliance do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:site_compliance, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :org_id,
          references(:organizations,
            column: :id,
            name: "site_compliance_org_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :enabled, :boolean, null: false, default: false
      add :require_at_publish, :boolean, null: false, default: false
      add :disclaimer, :text
      add :use_shared_rules, :boolean, null: false, default: true
      add :phrases, {:array, :text}, null: false, default: []
      add :phrase_severity, :text, null: false, default: "warning"

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:site_compliance, [:org_id], name: "site_compliance_one_per_org_index")
  end

  def down do
    drop constraint(:site_compliance, "site_compliance_org_id_fkey")

    drop_if_exists unique_index(:site_compliance, [:org_id],
                     name: "site_compliance_one_per_org_index"
                   )

    drop table(:site_compliance)
  end
end
