defmodule KilnCMS.Repo.Migrations.RequireOrgIdNotNull do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:block_embeddings) do
      modify :org_id, :uuid, null: false
    end

    alter table(:reference_edges) do
      modify :org_id, :uuid, null: false
    end

    alter table(:entries) do
      modify :org_id, :uuid, null: false
    end

    alter table(:published_artifacts) do
      modify :org_id, :uuid, null: false
    end

    alter table(:pages_versions) do
      modify :org_id, :uuid, null: false
    end

    alter table(:posts) do
      modify :org_id, :uuid, null: false
    end

    alter table(:entries_versions) do
      modify :org_id, :uuid, null: false
    end

    alter table(:posts_versions) do
      modify :org_id, :uuid, null: false
    end

    alter table(:pages) do
      modify :org_id, :uuid, null: false
    end
  end

  def down do
    alter table(:pages) do
      modify :org_id, :uuid, null: true
    end

    alter table(:posts_versions) do
      modify :org_id, :uuid, null: true
    end

    alter table(:entries_versions) do
      modify :org_id, :uuid, null: true
    end

    alter table(:posts) do
      modify :org_id, :uuid, null: true
    end

    alter table(:pages_versions) do
      modify :org_id, :uuid, null: true
    end

    alter table(:published_artifacts) do
      modify :org_id, :uuid, null: true
    end

    alter table(:entries) do
      modify :org_id, :uuid, null: true
    end

    alter table(:reference_edges) do
      modify :org_id, :uuid, null: true
    end

    alter table(:block_embeddings) do
      modify :org_id, :uuid, null: true
    end
  end
end
