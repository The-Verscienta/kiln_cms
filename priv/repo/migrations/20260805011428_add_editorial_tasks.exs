defmodule KilnCMS.Repo.Migrations.AddEditorialTasks do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:tasks, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :org_id,
          references(:organizations,
            column: :id,
            name: "tasks_org_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :content_type, :text, null: false
      add :content_id, :uuid, null: false
      add :due_on, :date
      add :note, :text
      add :status, :text, null: false, default: "open"
      add :completed_at, :utc_datetime_usec

      add :completed_by_id,
          references(:users,
            column: :id,
            name: "tasks_completed_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :overdue_notified_on, :date

      add :creator_id,
          references(:users,
            column: :id,
            name: "tasks_creator_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :assignee_id,
          references(:users,
            column: :id,
            name: "tasks_assignee_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end

    create index(:tasks, [:org_id, :assignee_id, :status], name: "tasks_assignee_lookup_index")

    create index(:tasks, [:org_id, :content_type, :content_id],
             name: "tasks_content_lookup_index"
           )
  end

  def down do
    drop constraint(:tasks, "tasks_org_id_fkey")

    drop constraint(:tasks, "tasks_completed_by_id_fkey")

    drop constraint(:tasks, "tasks_creator_id_fkey")

    drop constraint(:tasks, "tasks_assignee_id_fkey")

    drop_if_exists index(:tasks, [:org_id, :content_type, :content_id],
                     name: "tasks_content_lookup_index"
                   )

    drop_if_exists index(:tasks, [:org_id, :assignee_id, :status],
                     name: "tasks_assignee_lookup_index"
                   )

    drop table(:tasks)
  end
end
