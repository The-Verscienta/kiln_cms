defmodule KilnCMS.Repo.Migrations.EncryptWebhookSecrets do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:webhook_endpoints) do
      modify :secret, :text, null: true
      add :secret_encrypted, :binary
    end
  end

  def down do
    alter table(:webhook_endpoints) do
      remove :secret_encrypted
      modify :secret, :text, null: false
    end
  end
end
