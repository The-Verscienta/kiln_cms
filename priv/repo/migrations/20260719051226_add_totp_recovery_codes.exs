defmodule KilnCMS.Repo.Migrations.AddTotpRecoveryCodes do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:users) do
      add :totp_recovery_hashes, {:array, :text}, null: false, default: []
    end
  end

  def down do
    alter table(:users) do
      remove :totp_recovery_hashes
    end
  end
end
