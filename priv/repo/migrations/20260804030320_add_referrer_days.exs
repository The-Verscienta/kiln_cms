defmodule KilnCMS.Repo.Migrations.AddReferrerDays do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:referrer_days, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :org_id,
          references(:organizations,
            column: :id,
            name: "referrer_days_org_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :content_type, :text, null: false
      add :content_id, :uuid, null: false
      add :source, :text, null: false
      add :day, :date, null: false, default: fragment("CURRENT_DATE")
      add :hits, :bigint, null: false, default: 1

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create index(:referrer_days, [:org_id, :day], name: "referrer_days_trend_index")

    create unique_index(:referrer_days, [:org_id, :content_type, :content_id, :source, :day],
             name: "referrer_days_unique_referrer_day_index"
           )
  end

  def down do
    drop constraint(:referrer_days, "referrer_days_org_id_fkey")

    drop_if_exists unique_index(
                     :referrer_days,
                     [:org_id, :content_type, :content_id, :source, :day],
                     name: "referrer_days_unique_referrer_day_index"
                   )

    drop_if_exists index(:referrer_days, [:org_id, :day], name: "referrer_days_trend_index")

    drop table(:referrer_days)
  end
end
