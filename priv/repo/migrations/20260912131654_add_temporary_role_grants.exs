defmodule KilnCMS.Repo.Migrations.AddTemporaryRoleGrants do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:org_memberships) do
      add :granted_role, :text
      add :granted_role_expires_at, :utc_datetime_usec
    end

    alter table(:users) do
      add :granted_role, :text
      add :granted_role_expires_at, :utc_datetime_usec
    end
  end

  def down do
    alter table(:users) do
      remove :granted_role_expires_at
      remove :granted_role
    end

    alter table(:org_memberships) do
      remove :granted_role_expires_at
      remove :granted_role
    end
  end
end
