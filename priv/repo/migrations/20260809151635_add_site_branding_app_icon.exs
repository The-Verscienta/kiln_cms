defmodule KilnCMS.Repo.Migrations.AddSiteBrandingAppIcon do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:site_branding) do
      add :app_icon_url, :text
      add :app_icon_size, :bigint
    end
  end

  def down do
    alter table(:site_branding) do
      remove :app_icon_size
      remove :app_icon_url
    end
  end
end
