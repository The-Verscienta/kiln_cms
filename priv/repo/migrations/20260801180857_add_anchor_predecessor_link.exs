defmodule KilnCMS.Repo.Migrations.AddAnchorPredecessorLink do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:history_anchors) do
      add :prev_anchor_id, :uuid
      add :prev_anchor_digest, :text
    end
  end

  def down do
    alter table(:history_anchors) do
      remove :prev_anchor_digest
      remove :prev_anchor_id
    end
  end
end
