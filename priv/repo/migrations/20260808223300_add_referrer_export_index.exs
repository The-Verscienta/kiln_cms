defmodule KilnCMS.Repo.Migrations.AddReferrerExportIndex do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create index(:referrer_days, [:org_id, :day, :content_type, :content_id, :id],
             name: "referrer_days_export_index"
           )
  end

  def down do
    drop_if_exists index(:referrer_days, [:org_id, :day, :content_type, :content_id, :id],
                     name: "referrer_days_export_index"
                   )
  end
end
