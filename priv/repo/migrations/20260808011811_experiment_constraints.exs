defmodule KilnCMS.Repo.Migrations.ExperimentConstraints do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop constraint(
           :content_experiment_variants,
           "content_experiment_variants_experiment_id_fkey"
         )

    alter table(:content_experiment_variants) do
      modify :experiment_id,
             references(:content_experiments,
               column: :id,
               name: "content_experiment_variants_experiment_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all
             )
    end

    create index(:content_experiments, [:org_id, :content_type, :document_id],
             name: "content_experiments_one_running_per_document",
             unique: true,
             where: "state = 'running'"
           )
  end

  def down do
    drop_if_exists index(:content_experiments, [:org_id, :content_type, :document_id],
                     name: "content_experiments_one_running_per_document"
                   )

    drop constraint(
           :content_experiment_variants,
           "content_experiment_variants_experiment_id_fkey"
         )

    alter table(:content_experiment_variants) do
      modify :experiment_id,
             references(:content_experiments,
               column: :id,
               name: "content_experiment_variants_experiment_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end
  end
end
