{
  "name": "kiln-cms-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright browser E2E tests for the KilnCMS editor.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
